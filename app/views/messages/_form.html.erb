<%= form_for message do |f| %>

  <%= f.hidden_field :room_id, value: room.id %>

  <%= f.text_field :content, autocomplete: :off , placeholder: "Type a message..." ,class: "new_message__field"%>
  <%= f.button "send", type: 'submit' ,class: "new_message__submit" %>

<% end %>